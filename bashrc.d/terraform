# -*- mode: sh -*-

if [ $(command -v terraform) ]; then
  complete -C $(command -v terraform) terraform

  if [ $(command -v aws-vault) ]; then
    alias terraform='aws-vault exec "${AWS_PROFILE:-default}" -- terraform "$@"'
  fi
fi
